name: Copy Uptime Kuma Stable Image

on:
  workflow_dispatch:  # 仅手动触发

jobs:
  copy-image:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker
        run: |
          sudo apt-get update
          sudo apt-get install -y docker.io

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Pull and retag image
        run: |
          # 拉取源镜像
          docker pull ghcr.io/louislam/uptime-kuma:nightly2
          
          # 重新打标签为稳定版本
          docker tag ghcr.io/louislam/uptime-kuma:nightly2 ${{ secrets.DOCKERHUB_USERNAME }}/uptime-kuma:latest
          docker tag ghcr.io/louislam/uptime-kuma:nightly2 ${{ secrets.DOCKERHUB_USERNAME }}/uptime-kuma:stable
          
          # 推送镜像
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/uptime-kuma:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/uptime-kuma:stable
          
          echo "✅ 镜像已成功复制并推送为稳定版本！"
          echo "📦 镜像地址: ${{ secrets.DOCKERHUB_USERNAME }}/uptime-kuma:latest"
